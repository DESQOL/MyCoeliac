import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    button: {
        margin: 16,
        width: 175,
    },

    container: {
        alignItems: 'center',
        display: 'flex',
        flex: 1,
        marginTop: 50,
    },

    screenContainer: {
        flex: 1,
    },
});
