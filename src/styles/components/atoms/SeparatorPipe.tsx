import { StyleSheet } from 'react-native';
import { GrayLighter } from '../../config/Colors';

export default StyleSheet.create( {
    separator: {
        backgroundColor: GrayLighter,
        height: 1,
    }
});
